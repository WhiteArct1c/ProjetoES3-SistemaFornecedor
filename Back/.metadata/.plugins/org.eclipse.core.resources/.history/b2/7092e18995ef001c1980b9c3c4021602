package controller.web;

import java.io.IOException;

import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import model.EntidadeDominio;
import model.Servico;

public class ServicoVH implements IViewHelper {

	@Override
	public EntidadeDominio getEntidade(HttpServletRequest request) {
		
		Servico servico = new Servico(Integer.parseInt(request.getParameter("idServico")), request.getParameter("nomeServico"), 
				request.getParameter("descricaoServico"), Double.parseDouble(request.getParameter("precoServico")));
		
		return servico;
	}

	@Override
	public void setEntidade(HttpServletResponse response, HttpServletRequest request, Object msg) throws IOException {		
		response.sendRedirect(request.getContextPath() + "/CarregarServicos");
	}

}
